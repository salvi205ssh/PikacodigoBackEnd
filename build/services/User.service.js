"use strict";
/* import userModel from '../data/models/userModel'
interface UserPOJO {
    id: number;
    name: string;
    email: string;
    // Agrega aquí las propiedades adicionales que quieras copiar de UserPOJO a UserDTO
  }
  
  interface UserDTO {
    id: number;
    name: string;
    email: string;
    // Agrega aquí las propiedades adicionales que quieras copiar de UserPOJO a UserDTO
  }
  
  function convertToDTO(userPOJO: UserPOJO): UserDTO {
    const userDTO: UserDTO = {
      id: userPOJO.id,
      name: userPOJO.name,
      email: userPOJO.email,
      // Agrega aquí las propiedades adicionales que quieras copiar de UserPOJO a UserDTO
    };
    return userDTO;
  }
   */ 
